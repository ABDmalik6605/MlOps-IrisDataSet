stages:
  prepare:
    cmd: python prepare.py
    outs:
      - iris.csv
  train:
    cmd: python train.py
    deps:
      - iris.csv
      - train.py
    params:
      - split.test_size
      - train.n_estimators
      - train.random_state
    outs:
      - models/model.pkl
    metrics:
      - metrics/eval.json:
          cache: false
